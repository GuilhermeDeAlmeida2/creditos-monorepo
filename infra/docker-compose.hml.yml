version: '3.8'

services:

  api:
    build: ../api
    ports:
      - "8080:8080"
    environment:
      - API_PORT=8080
      - SPRING_PROFILES_ACTIVE=hml
      - CORS_ALLOWED_ORIGINS=https://hml.creditos.com.br,https://hml-creditos.com.br
      # Configurações do banco de dados - PostgreSQL HOMOLOGAÇÃO
      - DB_HOST=postgres-hml
      - DB_PORT=5432
      - DB_NAME=creditos_hml
      - DB_USER=postgres
      - DB_PASSWORD=${DB_PASSWORD}
      # Configurações específicas da aplicação
      - APP_ENVIRONMENT=homologation
      - TEST_FEATURES_ENABLED=false
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    networks:
      - creditos-network

  web:
    build: 
      context: ../web
      args:
        - ENVIRONMENT=homologation
    ports:
      - "3000:80"
    depends_on:
      api:
        condition: service_healthy
    networks:
      - creditos-network

networks:
  creditos-network:
    driver: bridge
